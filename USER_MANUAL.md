# 📖 دليل المستخدم | User Manual
## File Integrity Monitor System

مرحباً بك في الدليل التدريبي الشامل لنظام مراقبة تكامل الملفات. هذا المستند مصمم لتدريبك على استخدام النظام بكفاءة لحماية البيانات والتحقيق في الحوادث الأمنية.

---

## 📑 المحتويات | Table of Contents
1. [مقدمة عن النظام (Introduction)](#1-مقدمة-عن-النظام-introduction)
2. [التثبيت والإعداد (Installation)](#2-التثبيت-والإعداد-installation)
3. [بدء المراقبة (Start Monitoring)](#3-بدء-المراقبة-start-monitoring)
4. [فهم التنبيهات (Understanding Alerts)](#4-فهم-التنبيهات-understanding-alerts)
5. [التقارير الجنائية (Forensic Reporting)](#5-التقارير-الجنائية-forensic-reporting)
6. [استعادة الملفات (Data Recovery)](#6-استعادة-الملفات-data-recovery)

---

## 1. مقدمة عن النظام (Introduction)
يقوم هذا النظام بمراقبة المجلدات الحساسة على جهازك بشكل مستمر. في حال قام أي برنامج أو شخص بتعديل، حذف، أو إنشاء ملف، يقوم النظام فوراً بـ:
- **تنبيهك** بوجود تغيير.
- **تسجيل** الحدث في قاعدة البيانات.
- **تحديد المتسبب** (اسم البرنامج الذي قام بالتغيير).
- **أخذ نسخة احتياطية** تلقائية (في حال التعديل) لتمكين الاستعادة.

---

## 2. التثبيت والإعداد (Installation)

### المتطلبات المسبقة
- نظام تشغيل Windows 10 أو 11.
- تثبيت [Python](https://www.python.org/downloads/) (الإصدار 3.9 أو أحدث).

### خطوات التشغيل لأول مرة
1. **فتح المجلد**: افتح مجلد المشروع.
2. **إنشاء البيئة الافتراضية**:
   من شريط العنوان، اكتب `cmd` واضغط Enter، ثم اكتب الأوامر التالية:
   ```cmd
   python -m venv .venv
   .venv\Scripts\activate
   ```
3. **تحميل الملحقات**:
   ```cmd
   pip install -r requirements.txt
   ```
4. **تشغيل البرنامج**:
   ```cmd
   python app.py
   ```

---

## 3. بدء المراقبة (Start Monitoring)

بعد تشغيل البرنامج، ستظهر لك [الواجهة الرئيسية](README.md#%D8%AC%D9%88%D9%84%D8%A9-%D9%81%D9%8A-%D8%A7%D9%84%D9%86%D8%B8%D8%A7%D9%85--visual-tour).

1. **تحديد المسار**: اضغط على زر "Browse" لاختيار المجلد الذي تريد حمايته (مثلاً: مجلد المستندات المهمة).
2. **بدء الحماية**: اضغط على زر **"Start Monitoring"**.
   - ستتحول حالة النظام إلى اللون الأخضر (Active).
   - سيبدأ النظام بفحص أولي (Initial Scan) وحساب البصمات (Hashes) لجميع الملفات.

> **نصيحة**: لا تقم بإغلاق البرنامج، يمكنك تصغيره وسيعمل في الخلفية.

---

## 4. فهم التنبيهات (Understanding Alerts)

عند حدوث أي تغيير، سيظهر تنبيه في جدول الأحداث (Live Feed). الحقول المهمة هي:

| الحقل (Field) | الشرح (Description) | أمثلة |
| :--- | :--- | :--- |
| **File Path** | مسار الملف الذي تغير. | `C:\Docs\secret.txt` |
| **Event Type** | نوع الحدث. | `Modified` (تعديل), `Deleted` (حذف), `Created` (إنشاء) |
| **Actor** | **(الأهم)** البرنامج المسؤول عن التغيير. | `notepad.exe` (المستخدم فتح المفكرة), `unknown` (غير معروف), `python.exe` |
| **Time** | وقت وقوع الحدث بدقة. | `2025-01-14 10:30:01` |

---

## 5. التقارير الجنائية (Forensic Reporting)

لإصدار تقرير رسمي لتقديمه للإدارة أو للتدقيق:

1. اضغط على زر **"Export PDF Report"** في أعلى الواجهة.
2. سيتم إنشاء ملف PDF أنيق يحتوي على:
   - ملخص فترة المراقبة.
   - إحصائيات التغييرات.
   - قائمة مفصلة بجميع التهديدات المكتشفة باللون الأحمر.

يمكنك طباعة هذا التقرير أو إرساله عبر البريد الإلكتروني.

---

## 6. استعادة الملفات (Data Recovery)

إذا تم تعديل ملف "خطأ" أو بواسطة برمجية خبيثة (مثل Ransomware):

1. حدد الملف المعدل من قائمة التنبيهات.
2. اضغط **بالزر الأيمن** على التنبيه.
3. اختر **"Restore File"**.
4. سيقوم النظام فوراً باستبدال النسخة "المعدلة/التالفة" بالنسخة "الأصلية السليمة" التي تم حفظها تلقائياً قبل التعديل.

---

<p align="center">
  <b>انتهى الدليل التدريبي. لأي استفسارات تقنية، راجع فريق التطوير.</b>
</p>
